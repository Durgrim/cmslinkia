<div class="col py-2">
    <h1><u>Editar Usuario</u></h1><br>
    <p>ID = {{selectedUser._id}}</p>
    {{#each errors}}
        <div class="alert alert-danger">
            {{message}}
        </div>
    {{/each}}
    

    <form action="/admin/editUser/{{selectedUser._id}}" method="post">
        <div class="form-group">
            <label for="firstName">Nombre</label>
            <input name="firstName" class="form-control" id="firstName" type="text" value="{{selectedUser.firstName}}" required>
        </div>

        <div class="form-group">
            <label for="lastName">Apellido</label>
            <input name="lastName" class="form-control" id="lastName" type="text" value="{{selectedUser.lastName}}" required>
        </div>

       <div class="form-group">
            <label for="email">email</label>
            <input name="email" class="form-control" id="email" type="text" value="{{selectedUser.email}}" required>
        </div>

            <ul class="list-group list-group-horizontal">
                <li class="list-group-item">
                    <button class="btn btn-danger" type="submit">Actualizar</button>
    </form>
                </li>    
                <li class="list-group-item">
                    <form action="/admin/showUsers/" method="get">
                        <button class="btn btn-dark" type="submit">Volver atrás</button>
                    </form>
                </li>
        </ul>
</div>