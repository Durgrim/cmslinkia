<div class="col py-2">
    <h1><u>Dashboard</u></h1>
        <p class="lead">
            Bienvenido al menu de administración

            {{#if success}}
                <div class="alert alert-success">
                    {{success}}
                </div>
            {{/if}}
            {{#if error}}
                <div class="alert alert-danger">
                    {{error}}
                </div>
            {{/if}}
        </p>
</div>